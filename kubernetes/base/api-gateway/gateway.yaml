apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: api-gateway
  namespace: default
spec:
  gatewayClassName: nginx  # Assuming you have a GatewayClass named 'nginx'
  listeners:
    - name: http
      protocol: HTTP
      port: 80
